##functions стартовый модуль для импорта проекта

from docx import Document

#извлекает название товара из документа
def extract_product_names(path):

# создание документа
    document = Document(file_path)
    if len(doc.paragraphs) > 0:
        return


# открытие документа
document = Document('/path/to/document.docx')



def func1(n):
    """функция 1. извлечение названия товаров"""
    pass


def func2(n):
    """функция 2"""
    pass

